# Installation

## Frameworks
- [Next.js](https://nextjs.org/) is a React framework, it provides many features, including static generation, server-side rendering, dynamic routing, etc.
- [React](https://reactjs.org/) is a JavaScript library, it provides many features, including components, hooks, context, etc.
- [Tailwindcss](https://tailwindcss.com/) is a CSS framework, it provides many features, including utility-first, responsive design, dark mode, etc.
- [radix-ui](https://www.radix-ui.com/) is a React component library, it provides many features, including design system, component library, etc.
- [shadcn](https://shadcn.com/) is a React component library, it provides many features, including design system, component library, etc.
- [zustand](https://zustand.surge.sh/) is a state management library, it provides many features, including global state, local state, etc.
- [next/mdx](https://mdxjs.com/) is a markdown parser, it provides many features, including markdown, jsx, etc.
- [prisma](https://www.prisma.io/) is a database toolkit, it provides many features, including database schema, database migration, etc.
- [t3-env](https://t3-env.vercel.app/) is an environment variable management library, it can map environment variables to TypeScript classes.
- [next-auth](https://next-auth.js.org/) is an authentication framework, it provides many features, including login, register, forgot password, etc.
- [next-intl]() is an internationalization framework, it provides many features, including multi-language, multi-timezone, etc.
- [next-themes]() is a theme management library, it provides many features, including light theme, dark theme, etc.
- [react-email](https://react-email-editor.com/) is an email editor, it provides many features, including drag and drop, rich text, etc.
- [recharts](https://recharts.org/) is a chart library, it provides many features, including line chart, bar chart, pie chart, etc.
- [zod]() is a data validation library, it provides many features, including type validation, data conversion, data validation, etc.
- [zod]() is a data validation library, it provides many features, including type validation, data conversion, data validation, etc.

## 目录结构
```目录结构

  ├── LICENSE // 开源协议
  ├── README.md // 项目介绍
  ├── README_zh.md // 项目介绍-中文
  ├── components.json // 组件配置
  ├── next-env.d.ts // next 环境变量
  ├── next.config.mjs // next 配置
  ├── package-lock.json // 依赖版本锁定
  ├── package.json // 依赖配置
  ├── postcss.config.mjs // postcss 配置
  ├── prettier.config.cjs // prettier 配置
  ├── prisma // 数据库配置
  │    ├── migrations
  │    └── schema.prisma
  ├── public // 静态资源
  │    ├── next.svg
  │    └── vercel.svg
  ├── src // 源码
  │    ├── app // 应用
  │    │    ├── [locale] // 语言
  │    │    ├── api // api 接口
  │    │    ├── favicon.ico // 网站图标
  │    │    ├── globals.css // 全局样式
  │    │    └── layout.tsx // 全局布局
  │    ├── components // 组件
  │    │    └── ui // shadcn-ui 组件
  │    ├── config // 配置
  │    │    ├── auth.config.ts
  │    │    ├── dictionaries
  │    │    └── site.ts
  │    ├── env.mjs // 环境变量
  │    ├── i18n.ts // 国际化
  │    ├── lib // 工具
  │    │    ├── email.ts
  │    │    ├── i18n-navigation.ts
  │    │    ├── primsa.ts
  │    │    └── utils.ts
  │    ├── mdx-components.tsx // mdx 组件
  │    ├── middleware.ts // 中间件
  │    ├── providers // provider
  │    │    └── gitee.ts
  │    ├── store // 状态管理
  │    │    └── index.ts
  │    └── templates // email 模板
  │        └── email.tsx
  ├── tailwind.config.ts // tailwind 配置
  ├── tsconfig.json // typescript 配置
  └── yarn.lock // 依赖版本锁定

```